# saved executables (~40MB SBCL images). The glob matters: a stray `sxsh2'
# from a side-by-side build was committed once because only /sxsh was listed.
/sxsh
/sxsh[0-9]*
/*.image

# ASDF/SBCL build output
*.fasl
*.fas
*.lib
.cache/
