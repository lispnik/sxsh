# saved executables (~40MB SBCL images). The glob matters: a stray `posh2'
# from a side-by-side build was committed once because only /posh was listed.
/posh
/posh[0-9]*
/*.image

# ASDF/SBCL build output
*.fasl
*.fas
*.lib
.cache/
