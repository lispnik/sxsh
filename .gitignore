# Built executables (~40MB SBCL images) all land in bin/, so one directory
# covers them. An earlier layout put the binary at the repo root and listed it
# by name, which let a stray side-by-side build get committed.
/bin/

# ASDF/SBCL build output
*.fasl
*.fas
*.lib
.cache/
